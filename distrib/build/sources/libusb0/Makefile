# SmoothWall Build system.
#
# (c) SmoothWall Ltd 2005
#
# This code is distributed under the terms of the GPL v2.

include ../Makefile.conf

US_PKG = libusb
PACKAGE = $(US_PKG)0
VER_MM = 0.1
VERSION = $(VER_MM).12
TARBALL = $(US_PKG)-$(VERSION)
EXTENSION = .tar.gz

BASE_URL = http://heanet.dl.sourceforge.net/project/$(US_PKG)/$(US_PKG)-$(VER_MM)\ \(LEGACY\)/$(VERSION)

include ../Makefile.rules

