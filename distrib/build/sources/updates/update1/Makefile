UPDATE_PACKAGES = acpica apcupsd at bash clamav coretree daq dosfstools httpd \
		  iperf kbd libsmooth+setup openssl perl-mods smoothd snort squid

UPDATE_ID = 001
UPDATE_NAME = rc5/6 test update

include ../Makefile.update
